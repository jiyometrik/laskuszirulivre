\ProvidesPackage{zesztof}
\usepackage{amsfonts, amsthm, amsmath, amssymb}
\usepackage{mathtools}

\usepackage{siunitx}
\usepackage{physics}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{tikz-cd}
\usepackage{xcolor}

% \usepackage{AlegreyaSans}
\usepackage[scale=0.9]{sourcecodepro}
\usepackage{libertinus}

\usepackage{microtype}

% \usepackage{gillius}

% \usepackage{fbb}
% \usepackage{cabin}
% \usepackage{cochineal}

\def\bf#1{\textbf{#1}}
\def\it#1{\textit{#1}}

\def\bN{\mbb{N}}
\def\bC{\mbb{C}}
\def\bR{\mbb{R}}
\def\bQ{\mbb{Q}}
\def\bZ{\mbb{Z}}

\usepackage{setspace}
\onehalfspacing

\usepackage[most]{tcolorbox}
\newcounter{tatoeba}
\def\exampletext{Tatoeba}
\NewDocumentEnvironment{tatoeba}{ O{} }{
	\colorlet{colexam}{red!55!black}
	\newtcolorbox[use counter=tatoeba]{tatoebabox}{%
		empty,% Empty previously set parameters
		title={\exampletext\space\thechapter.\thetcbcounter #1},% use \thetcbcounter to access the testexample counter text
		attach boxed title to top left,
		minipage boxed title,
		boxed title style={empty,size=minimal,toprule=0pt,top=4pt,left=3mm,overlay={}},
		coltitle=colexam,fonttitle=\bfseries\sffamily,
		before=\par\medskip\noindent,parbox=false,boxsep=0pt,left=3mm,right=0mm,top=2pt,breakable,pad at break=0mm,
		before upper=\csname @totalleftmargin\endcsname0pt,
		overlay unbroken={\draw[colexam,line width=.5pt] ([xshift=-0pt]title.north west) -- ([xshift=-0pt]frame.south west); },
		overlay first={\draw[colexam,line width=.5pt] ([xshift=-0pt]title.north west) -- ([xshift=-0pt]frame.south west); },
		overlay middle={\draw[colexam,line width=.5pt] ([xshift=-0pt]frame.north west) -- ([xshift=-0pt]frame.south west); },
		overlay last={\draw[colexam,line width=.5pt] ([xshift=-0pt]frame.north west) -- ([xshift=-0pt]frame.south west); },%
	}
	\begin{tatoebabox}
		}{
	\end{tatoebabox}\endlist
}

\newcounter{svarna}
\def\answertext{Svarna}
\NewDocumentEnvironment{svarna}{ O{} }{
	\colorlet{colexam}{blue!55!black}
	\newtcolorbox[use counter=svarna]{svarnabox}{%
		empty,% Empty previously set parameters
		title={\answertext\space\thechapter.\thetcbcounter #1},% use \thetcbcounter to access the testexample counter text
		attach boxed title to top left,
		minipage boxed title,
		boxed title style={empty,size=minimal,toprule=0pt,top=4pt,left=3mm,overlay={}},
		coltitle=colexam,fonttitle=\bfseries\sffamily,
		before=\par\medskip\noindent,parbox=false,boxsep=0pt,left=3mm,right=0mm,top=2pt,breakable,pad at break=0mm,
		before upper=\csname @totalleftmargin\endcsname0pt,
		overlay unbroken={\draw[colexam,line width=.5pt] ([xshift=-0pt]title.north west) -- ([xshift=-0pt]frame.south west); },
		overlay first={\draw[colexam,line width=.5pt] ([xshift=-0pt]title.north west) -- ([xshift=-0pt]frame.south west); },
		overlay middle={\draw[colexam,line width=.5pt] ([xshift=-0pt]frame.north west) -- ([xshift=-0pt]frame.south west); },
		overlay last={\draw[colexam,line width=.5pt] ([xshift=-0pt]frame.north west) -- ([xshift=-0pt]frame.south west); },%
	}
	\begin{svarnabox}
		}{
	\end{svarnabox}\endlist
}


\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	breaklinks=true,
	% linkcolor=blue,
	% filecolor=magenta,      
	% urlcolor=cyan,
	% pdftitle={Overleaf Example},
	% pdfpagemode=FullScreen,
}
