\ProvidesPackage{zesztof}

% ---------
% laskukaku
% ---------
\RequirePackage{amsfonts, amsthm, amsmath, amssymb}
\RequirePackage{mathtools}

% --------------------
% farzapol + laskuriso
\RequirePackage{siunitx}
\RequirePackage{pgfplots}
\pgfplotsset{compat=1.19}

% -----
% varje
% -----
\RequirePackage{xcolor}
\definecolor{zedvera}{RGB}{0, 0, 90}
\definecolor{midori}{RGB}{187, 219, 155}
\definecolor{gris}{RGB}{167, 162, 169}

\RequirePackage{unicode-math}
\setmainfont{TeX Gyre Pagella}
\setsansfont{TeX Gyre Heros}
\setmathfont{Asana Math}
\setmonofont{Inconsolata}
\RequirePackage{microtype}

% ----------------
% simpel dvaimanen
% ----------------
\providecommand{\ds}[1]{\textbf{#1}}
\providecommand{\vk}[1]{\textit{#1}}
\providecommand{\mono}[1]{\texttt{#1}}
\providecommand{\us}[1]{\underline{#1}} % unnasen
\providecommand{\lds}[1]{\mathbf{#1}} % lasku -- da se
\providecommand{\ldd}[1]{\mathbb{#1}} % lasku -- da da (100)
\providecommand{\ldj}[1]{\mathfrak{#1}} % lasku -- da (d)jong
\providecommand{\lko}[1]{\text{#1}} % kotoba na laskuspesu

\providecommand{\ln}{\ldj{N}}
\providecommand{\lc}{\ldj{C}}
\providecommand{\lr}{\ldj{R}}
\providecommand{\lq}{\ldj{Q}}
\providecommand{\lz}{\ldj{Z}}

\providecommand{\tc}[1]{\left(#1\right)}
\providecommand{\tl}[1]{\left[#1\right]}
\providecommand{\tx}[1]{\left\{#1\right\}}
\providecommand{\gla}[1]{\left\langle#1\right\rangle} % gulak-sen
\providecommand{\tum}[1]{\left|#1\right|} % tumam-sen
\providecommand{\dva}[3]{#1\colon#2\mapsto#3} % dvaima
\providecommand{\pol}[1]{\left\lfloor#1\right\rfloor} % polsen
\providecommand{\ele}[1]{\left\lceil#1\right\rceil} % elesen

\RequirePackage{setspace}
\onehalfspacing

\providecommand{\@mathsect}[3][section]{\csname#1\endcsname[\texorpdfstring{#2}{#3}]{{\boldmath #2}}}
\providecommand{\msection}[2]{\@mathsect[section]{#1}{#2}}
\providecommand{\msubsection}[2]{\@mathsect[subsection]{#1}{#2}}
\providecommand{\msubsubsection}[2]{\@mathsect[subsubsection]{#1}{#2}}

\providecommand{\silentchapter}[1]{
	\chapter*{#1}
	\markboth{#1}{#1}
	\addcontentsline{toc}{chapter}{#1}
}

% \def\tcb@restore@footnote{%
%   \def\@mpfn{footnote}%
%   \def\thempfn{\arabic{footnote}}%
%   \let\@footnotetext\tcb@footnote@collect
% }
%
% % collect footnote text
% \long\def\tcb@footnote@collect#1{%
%   \expandafter\gappto\expandafter\tcb@footnote@acc\expandafter{%
%     \expandafter\footnotetext\expandafter[\@thefnmark]{#1}%
%   }%
% }
%
% \def\tcb@footnote@use{%
%   \tcb@footnote@acc
%   \global\let\tcb@footnote@acc\@empty
% }
% \global\let\tcb@footnote@acc\@empty

\makeatletter
\newcommand{\alignfoot}[1]{%
  \ifmeasuring@
  \else
    \iffirstchoice@
      \footnote{#1}%
    \fi
  \fi}
\makeatother

\RequirePackage[most]{tcolorbox}
\tcbset{
	breakable,
	before skip=4mm,
	after skip=4mm,
	left skip=2pt,
	right skip=2pt,
	toptitle=2mm,
	bottomtitle=1mm,
	bottom=2mm,
	left=2pt,
	right=2pt,
	boxrule=0pt,
	bottomrule=1pt,
	toprule=1pt,
	sharp corners,
}

\newtcolorbox[auto counter, number within=chapter]{tatoeba}{%
	title=Tatoeba~\thetcbcounter,
	fonttitle=\bfseries\sffamily,
	enhanced,
	breakable,
	colback=gris!25!white,
	colbacktitle=gris!50!white,
	coltitle=black,
}

\newtcolorbox[auto counter, number within=chapter]{svarna}{%
	title=Svarna~\thetcbcounter,
	fonttitle=\bfseries\sffamily,
	enhanced,
	breakable,
	colback=gris!25!white,
	colbacktitle=gris!50!white,
	coltitle=black,
}

\newtcolorbox[auto counter, number within=chapter]{ljenszinen}{%
	title=Ljenszinen~\thetcbcounter,
	fonttitle=\bfseries\sffamily,
	enhanced,
	breakable,
	colback=midori!50!white,
	colbacktitle=midori,
	coltitle=black,
}

\RequirePackage{hyperref}
\hypersetup{
	hidelinks,
	colorlinks,
	breaklinks=true,
	urlcolor=zedvera,
	citecolor=zedvera,
	linkcolor=zedvera,
	bookmarksopen=false,
	pdftitle={Laskuszirulivre},
	pdfauthor={Zsijozsin},
}

\allowdisplaybreaks
